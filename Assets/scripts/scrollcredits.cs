using UnityEngine;
using System.Collections;

public class scrollcredits : MonoBehaviour {

	public float scrollspeed;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//this.transform.position.x += scrollspeed;
	}
}
